To be tested for possible properties implementation

OUTFILE=$(mktemp output-XXXX.csv)
echo $OUTFILE >> $OUTPUT__output_properties ## Same link or Different

##Path to the block home directory
var_PATH=$BLOCK_HOME/scripts/*.csv

##Path to the new file (may include full path or filename)
var_newfile=newfile.csv

##Content to be transferred from temp to new file
cat $OUTFILE > $var_newfile  ### possible error on no such file

for f in $var_PATH;
do
###run the command sequence
done



